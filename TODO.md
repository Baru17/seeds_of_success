# TODO

- [x] Update `tutor-dashboard.html`: replace Progress (1–5) dropdown in “Log a Session” form with Time input (AM/PM via select removed previously; now using numeric hour/minute + AM/PM).
- [x] Update “Sessions” table (recent sessions) to show:
  - Student dropdown for each row
  - Topic checkboxes for each row
  - Live progress update: if 3 topics ticked => `3 / 5` and progress bar width accordingly
- [x] Add CSS/JS for checkbox -> progress calculation per row.



